#!/usr/bin/awk -f
BEGIN { FS = "[ )]" }
{
    if ($1 == "create") {
        if ($3 == "patient_id") {
            print "create table patient_id (pk bigint generated by default as identity, pat_id varchar(64) not null, pat_id_type_code varchar(64), entity_id varchar(64) not null, pat_name varchar(64) not null, entity_uid varchar(64) not null, entity_uid_type varchar(64) not null, version bigint, patient_fk bigint not null, primary key (pk));"
        } else {
            print $0
        }
    } else {
        print $0
    }
}
