import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DicomStudiesTableComponent } from './dicom-studies-table.component';

describe('DicomStudiesTableComponent', () => {
/*  let component: DicomStudiesTableComponent;
  let fixture: ComponentFixture<DicomStudiesTableComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DicomStudiesTableComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DicomStudiesTableComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});
