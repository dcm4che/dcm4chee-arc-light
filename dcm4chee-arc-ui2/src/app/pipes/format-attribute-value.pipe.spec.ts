/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FormatAttributeValuePipe } from './format-attribute-value.pipe';

describe('Pipe: FormatAttributeValue', () => {
  it('create an instance', () => {
    let pipe = new FormatAttributeValuePipe();
    expect(pipe).toBeTruthy();
  });
});
