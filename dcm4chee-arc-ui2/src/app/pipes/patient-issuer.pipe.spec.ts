/* tslint:disable:no-unused-variable */

import { PatientIssuerPipe } from './patient-issuer.pipe';

describe('Pipe: PatientIssuer', () => {
  it('create an instance', () => {
    let pipe = new PatientIssuerPipe();
    expect(pipe).toBeTruthy();
  });
});
